version: "2"
servers:
- engine: postgresql
  uri: ${DATABASE_URI}
sql:
- schema: "db/schema/"
  queries: "db/queries/"
  engine: postgresql
  gen:
    go:
      out: "internal/database"
      emit_json_tags: true
  database:
    uri: ${DATABASE_URI}